// random integer in the range of 50..100
let min = 50;
let max = 100;
console.log(Math.floor(Math.random() * (max - min + 1)) + min);

// random integer in the range of 0..100
console.log(Math.floor(Math.random() * 101))
